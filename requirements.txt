# DEPRECATED — this file is no longer the source of truth.
#
# This project uses uv. To install all dependencies run:
#
#   uv sync          # runtime + default dev group
#   uv sync --group dev  # explicit dev extras
#
# Runtime and dev dependencies are declared in pyproject.toml.
# The pinned lockfile is uv.lock (committed to version control).
