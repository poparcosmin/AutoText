const PRODUCTION = true; // Set to true to use live server (autotext.zua.ro)

const CONFIG = {
    PRODUCTION: PRODUCTION,
    API_URL: PRODUCTION ? "https://autotext.zua.ro/api" : "http://127.0.0.1:8000/api"
};
